export const getStatData = state => state.stats;

// function* example {
//   for (let key in stats) {
//     if (stats[key].rate > 0) {
//       this.props.incrementValue(key);
//     }
//   }
// }

// for (let key in stats) {
//   if (stats[key].rate > 0) {
//     this.props.incrementValue(key);
//   }
// }
